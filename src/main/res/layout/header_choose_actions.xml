<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/material_header_content_size">

        <RelativeLayout
            android:id="@+id/choose_actions_header_circle_view"
            android:layout_width="@dimen/header_goal_icon_image_size"
            android:layout_height="@dimen/header_goal_icon_image_size"
            android:gravity="center"
            android:layout_gravity="center|bottom"
            android:contentDescription="@string/behavior_image_description"
            android:background="@drawable/circle_gray"
            android:layout_marginBottom="40dp">

            <ImageView
                android:id="@+id/choose_actions_header_imageview"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_margin="0dp"
                android:src="@drawable/ic_action_compass_white" />

        </RelativeLayout>
    </FrameLayout>

</RelativeLayout>